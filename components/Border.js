import { View } from "react-native"

export const Border = () => {
  return (
    <View
      style={{
        borderWidth: 1,
        borderColor: "gray",
        borderRadius: 50,
        flexGrow: 1,
      }}>
    </View>
  )
}
